# Ревизия проекта 2026-07-30

## Итог

Накопленный checkpoint проверен перед commit/push. Код, TaskRegistry, Job
Worker, Telegram-бот, WB runtime и официальные API Ozon/WB работоспособны.

Блокирующих дефектов кода для commit не найдено. Выявлен один актуальный
операционный риск: Ozon web-сессия Vital Shevron утратила авторизацию.
Keeper/CDP и refresh timer работают, но Ozon переводит страницы на signin.
Seller API и Performance API при этом исправны. Во время ревизии marketplace
write не выполнялся.

## Проверенный объем

- карточный Layer 2 -> Layer 3 -> plan/apply/verify и синхронизация статусов;
- seller SKU, WB card create, media policy и catalog mapping;
- Ozon/WB inbox, отзывы, вопросы, уведомления и cleanup;
- Ozon cookie normalization, keeper и session refresh;
- Ozon CPC, акции, цены, `Звёздные товары` и распродажа залежалых SKU;
- WB минимальные цены автоакций, основной action optimizer, CPC и распродажа;
- Telegram Job Worker, TaskRegistry, отчеты и owner-facing уведомления;
- постоянные runbook, API endpoint cards, followups и recommendations.

## Код и контракты

- полный `pytest`: `494 passed`;
- TaskRegistry: `84` задачи;
- Telegram-enabled задач: `22`;
- задач без `runbook_path`: `0`;
- policy issues TaskRegistry: `0`;
- все project systemd unit-файлы установлены без расхождений с репозиторием;
- runtime secrets, cookies, storage state и raw marketplace data исключены
  `.gitignore`;
- воспроизводимый `ruff` toolchain пока не зафиксирован, поэтому lint остается
  открытой рекомендацией `VS-REC-117`.

## Read-only preflight

Запуск:

```text
run_id: project_revision_20260730T1750
mode: read_only
overall_status: error
```

Подтверждено:

- Ozon Seller API `/v3/product/list`: `ok`;
- Ozon Performance API token readiness: `ok`;
- WB Content API `/content/v2/get/cards/list`: `ok`;
- master catalog: `814` строк, `319` подтвержденных пар, barcode mismatch `0`;
- WB ЛК и Promotion: `ok`, storage state обновлен;
- Ozon CDP `127.0.0.1:9544`: активен, contour guard указывает на профиль
  Vital Shevron;
- Ozon web ЛК: `needs_login` на dashboard, analytics, products и prices.

Preflight получил `error` только из-за Ozon web-авторизации. Значения cookies,
токенов и auth headers в отчет не выводились.

## Runtime и systemd

На момент проверки:

- Telegram bot: `active/enabled`;
- Job Worker timer: `active/enabled`, очередь пуста;
- Ozon keeper: `active/enabled`;
- Ozon и WB refresh timers: `active/enabled`;
- WB refresh: `ok`;
- Ozon refresh service: `failed`, подтвержденная причина `Login required`;
- liquidation daily reminder: `active/enabled`, следующий запуск
  `2026-07-31 09:00 МСК`;
- активных resource leases: `0`;
- jobs: `91 success`, `17 partial_success`, `7 failed`, активных `0`;
- approvals: `34 applied`, `3 applying_unknown`.

Три `applying_unknown` являются историческим audit tail: один WB actions apply
от 2026-07-24 и два Ozon inbox apply от 2026-07-27/28. Они не находятся в
очереди и не должны автоматически повторяться.

## Накопленный функциональный checkpoint

1. Ozon cookies нормализуются атомарно и не размножаются по доменным alias.
2. Карточный batch apply синхронизирует seller SKU, WB create, media и локальные
   Layer 2/Layer 3 статусы после verify.
3. WB action optimizer выбирает максимальную допустимую акционную цену не ниже
   minimum; ручная схема скидок сохранена отдельным режимом.
4. WB price grid и minimum автоакций применены к целевой категории с verify.
5. Ozon dormant-когорта `138/138` находится в CPC; WB первый этап распродажи
   применен к `163` карточкам.
6. Ежедневный read-only reminder контролирует обе распродажи по отдельным
   hard stop.

## Остаточные риски

- для Ozon web ЛК нужен новый интерактивный вход или свежие действующие cookies;
  refresh не может восстановить серверно отозванную сессию;
- до реализации `VS-REC-142` потеря web-авторизации не отправляет владельцу
  отдельное state-change уведомление;
- `3 applying_unknown` требуют отдельного audit cleanup, но не повторного apply;
- второй price stage WB для `18` строк требует fresh dry-run и отдельного owner
  approval;
- lint toolchain `ruff` не зафиксирован;
- ряд специализированных operational scripts еще не зарегистрирован в
  TaskRegistry и запускается только по runbook.

## Точка продолжения

1. Восстановить Ozon web-авторизацию и повторить `status-preflight`.
2. Не повторять исторические `applying_unknown` автоматически.
3. Ежедневно контролировать Ozon/WB liquidation-когорты.
4. Для WB второго price stage построить fresh dry-run и запросить отдельное
   подтверждение владельца.
5. Продолжить развитие `VS-REC-117`, `VS-REC-126`, `VS-REC-132` и
   `VS-REC-142` отдельными задачами.
