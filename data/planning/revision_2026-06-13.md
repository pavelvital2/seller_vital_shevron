# Revision 2026-06-13

## Цель

Полная ревизия проекта после настройки и выполнения marketplace-контуров Ozon и
WB:

- привести проект в порядок;
- проверить перелинковку документов, CLI и тестов;
- очистить технический мусор;
- зафиксировать состояние в git;
- сформулировать дальнейшие рекомендации.

## Проверки

Выполнить перед commit:

```bash
PYTHONPATH=src /home/Codex/agent-tools/python/bin/python -m compileall -q src tests
PYTHONPATH=src /home/Codex/agent-tools/python/bin/python -m takterra_agent.cli --help
PYTHONPATH=src /home/Codex/agent-tools/python/bin/python -m takterra_agent.cli plan-wb-promotion-bids --help
PYTHONPATH=src /home/Codex/agent-tools/python/bin/python -m takterra_agent.cli apply-wb-promotion-bids --help
PYTHONPATH=src /home/Codex/agent-tools/python/bin/pytest
git diff --check
git status --short --branch
```

## Что приведено в порядок

- README обновлен под существующий private GitHub repository.
- `AGENTS.md` ссылается на актуальный handoff 2026-06-13.
- `project_map.md` обновлен под новые Ozon/WB контуры.
- Добавлен актуальный handoff `fresh_agent_handoff_2026-06-13.md`.
- Runtime reports и raw snapshots остаются ignored.
- Технический мусор `__pycache__`, `*.pyc`, `.pytest_cache` очищается перед
  commit.

## Реализованные контуры

### Ozon

- Ozon Elastic apply: отдельный apply-контур с fresh plan, drift-check и verify.
- Ozon CPC optimization: SKU-level dry-run с current bids, target bid и дельтой.
- Ozon CPC apply: Performance API, fresh current-bid snapshot, drift-check,
  skipped rows и verify.

### WB

- WB actions discounts: схема Vital Shevron `70-55-55`.
- WB promotion report: read-only через official Promotion API.
- WB promotion bid plan: current API bid, target bid, bid delta по nmID.
- WB promotion bid apply: `PATCH /api/advert/v1/bids`, fresh report, fresh plan,
  drift-check, wait, verify.

Последний WB promotion apply:

```text
run_id: wb_promotion_bids_apply_20260613T001504
action: scale_candidate only
applied_rows: 22
bid_change_sum_applied: +1.80
verify: ok
```

## Замечания

- `src/takterra_agent` сохраняет старое имя package. Переименование остается
  отдельным этапом, потому что оно затрагивает весь импортный граф.
- Снижения WB promotion ставок не применялись. Для них нужен отдельный
  approved apply.
- WB candidate `589057650` не имел текущей ставки в Promotion API settings и
  был пропущен.
- `status-preflight` может возвращать `warning`, если LK-контуры не проверяются
  или требуют refresh; API-only операции при этом могут быть допустимы, если
  конкретный task явно использует `include_lk=False`.

## Рекомендации

1. Добавить scheduled post-apply monitoring для WB promotion: через 24 и 48
   часов сравнивать расход, заказы, ДРР и CPC по 22 повышенным nmID.
2. Встроить Ozon CPC и WB promotion summary в ежедневный seller report.
3. Сделать общий `approved package` формат для всех write-операций, чтобы
   Telegram-бот мог безопасно показывать diff и принимать подтверждение.
4. Добавить stock/preflight фильтр перед повышением рекламных ставок: не
   усиливать товары без достаточного остатка.
5. Сформировать отдельный optimization loop: read-only stats -> candidate plan
   -> owner approval -> apply -> delayed verify -> effectiveness report.
6. Перевести LK refresh на `systemd --user` timers и убрать зависимость от
   legacy watchdog-процессов.
7. Спланировать rename `takterra_agent` в нейтральное имя отдельным commit после
   стабилизации marketplace-контуров.

## Дополнительная ревизия 2026-06-13 15:56 MSK

### Цель

Привести проект в порядок после блока работ по ценам, поисковым запросам,
отзывам/вопросам и картам ЛК Ozon/WB.

### Проверенные изменения

- Добавлены постоянные карты ЛК:
  - `data/planning/ozon_cabinet_map.md`;
  - `data/planning/wb_cabinet_map.md`.
- Добавлены и связаны новые runbook-и:
  - `data/planning/pricing_runbook.md`;
  - `data/planning/search_queries_runbook.md`;
  - `data/planning/wb_parser_positions_runbook.md`.
- `AGENTS.md`, `README.md` и `project_map.md` обновлены ссылками на новые
  постоянные документы.
- `recommendations_index.md` дополнен рекомендациями `VS-REC-021` -
  `VS-REC-028`.
- `reviews_questions_runbook.md` дополнен правилами формы отчета,
  обязательными оценками, approved-пакетом, verify и внештатными ситуациями.
- Исправлен WB payload для ответа на вопрос:
  `{"id": "...", "answer": {"text": "..."}, "state": "wbRu"}`.

### Проверки

Выполнено:

```text
sessions status: ok
status-preflight: ok
markdown links: ok
backtick path audit: ok, ожидаемые runtime/template paths не считаются ошибкой
compileall src/tests: ok
CLI help: ok
pytest: 52 passed
git diff --check: ok
```

Проверка индекса перед commit должна подтвердить, что в git не добавлены:

```text
.env
.sessions/
tmp/auth/
data/runs/ operational files
data/pending/ operational files
data/approved/ operational files
raw/processed catalog snapshots
```

### Очистка

Перед commit удалить:

```text
__pycache__/
*.pyc
.pytest_cache/
```

### Рекомендация по развитию

`VS-REC-028`: автоматизировать read-only snapshot меню ЛК Ozon/WB и diff с
`ozon_cabinet_map.md` / `wb_cabinet_map.md`, чтобы fresh-агенты видели
устаревшие ссылки до начала операции.
