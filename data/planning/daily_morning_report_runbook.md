# Daily Morning Report Runbook

## Команды

Технический отчет:

```bash
PYTHONPATH=src python3 -m seller_agent.cli daily-morning-report
```

Селлерский v2:

```bash
PYTHONPATH=src python3 -m seller_agent.cli daily-morning-report --seller-v2
```

Селлерский v3 по утвержденному Telegram-шаблону:

```bash
PYTHONPATH=src python3 -m seller_agent.cli daily-morning-report --seller-v3
```

## Правило до унификации SKU

Отчет должен показывать Ozon и WB как отдельные контуры. Объединение по товару
разрешено только там, где есть подтвержденный mapping.

С 2026-06-24 отчет читает `data/catalog/unified/products.json` как основной
product-level слой, если файл существует. Этот слой используется для сводки
единого каталога и для вывода внутренних `internal_sku` рядом с Ozon/WB
артикулами в товарных примерах. Если unified catalog отсутствует, отчет
возвращается к legacy `data/catalog/processed/master_catalog.json`.

Telegram-вывод ежедневного отчета строить по общему стандарту
`data/planning/chat_report_templates.md`: chat-summary должен быть достаточным
для просмотра с телефона без открытия полного файла.

Перед сборкой и отправкой ежедневного отчета агент обязан проверить
`data/planning/followups.md`. Если там есть pending-задачи с датой текущего
дня, их нужно вывести в отдельном блоке отчета и выполнить в рамках утреннего
цикла, если это read-only контроль. Если задача требует write-действий,
сначала подготовить dry-run/review и запросить подтверждение владельца.

## Обязательный формат ежедневного отчета

Ежедневный отчет строится за одну дату: предыдущий календарный день по
`Europe/Moscow`, период `00:00-23:59`.

Все ключевые блоки должны показывать Ozon и WB рядом:

- единый каталог: всего товаров, с внутренним артикулом, связанные Ozon+WB,
  только Ozon, только WB, активные на Ozon/WB;
- заказы за вчера: количество и сумма;
- выкупы за вчера: количество и сумма;
- отмены за вчера;
- отзывы и вопросы за вчера `00:00-23:59`;
- отзывы и вопросы, которые требуют внимания на текущий момент: ответ или
  просмотр;
- Ozon-уведомления/сообщения из `https://seller.ozon.ru/app/messenger?group=customers_v2`:
  - сколько новых/непрочитанных;
  - сколько вопросов/чатов покупателей требуют ответа;
  - сколько важных площадочных уведомлений нужно передать владельцу;
  - кратко: какие изменения Ozon важны;
  - что отсеяно как шум/мусор;
- остатки на текущий момент;
  - для WB не называть отсутствующую строку в legacy stock endpoint нулевым
    остатком; писать отдельно: `WB-артикулы без строки в источнике остатков`;
  - в полном файле отчета показывать WB товары с подтвержденным нулевым
    остатком и отдельную выборку WB-артикулов без строки в источнике остатков
    с названием товара и внутренним `internal_sku`, если они есть в unified
    catalog или fallback master catalog;
- акции на текущий момент: какие активны, в каких участвуем, сколько товаров
  участвует, сколько не участвует и почему;
  - причины неучастия/проблем по акциям обязательно раскладывать минимум на:
    `нулевой остаток`, `участие в акции выше порога`,
    `нет акций для этого товара`;
  - для WB `участие в акции выше порога` не смешивать с `не участвует`: это
    товары, которые есть в акциях, но требуют корректировки скидки по схеме;
  - если товар отсутствует в legacy stock endpoint WB, это писать отдельно как
    `нет строки в источнике остатков`, не приравнивать к нулю без
    подтверждения;
- текущие поставки: есть или нет; если есть, показать стадии отдельно для Ozon
  и WB:
  - формируется;
  - готова к отгрузке;
  - на точке отгрузки;
  - в пути;
  - на складе приемки;
  - приемка.

Если источник данных не подключен, вернул ошибку или не дает нужный показатель,
в отчете писать `не подтверждено` и указывать причину. Нельзя подставлять
догадки вместо фактических данных.

Если ежедневный отчет получается неполным из-за недоступного ЛК, слетевшей
сессии или ошибки LK/CDP fallback, агент не должен сразу отдавать неполный
отчет как итоговый. Сначала нужно прямо предложить владельцу восстановить
подключение к соответствующему ЛК и пересобрать отчет. Неполный отчет можно
отправлять только если владелец подтвердил, что отчет нужен сейчас в текущем
неполном виде, либо если восстановление ЛК невозможно в рамках текущего
цикла. В итоговом сообщении обязательно указать, какие блоки остались
`не подтверждено` и почему.

## Источники, которые нужно довести до v3

- Ozon/WB выкупы и отмены за точный период `00:00-23:59`.
- Ozon отзывы/вопросы за вчера и текущие требующие внимания.
- Ozon уведомления/сообщения из Messenger: покупательские вопросы, важные
  сообщения площадки и шум.
- Ozon/WB action participation с причинами неучастия товаров.
- Ozon/WB supply status по стадиям поставки.

До подключения этих источников ежедневный отчет может иметь статус `warning`,
даже если API и сессии работают.

## Реализация v3

`--seller-v3` уже должен:

- считать дату отчета по `Europe/Moscow`;
- использовать предыдущий календарный день как период `00:00-23:59`;
- показывать Ozon/WB рядом в таблицах;
- брать Ozon заказы из Seller API `/v1/analytics/data`;
- брать Ozon выкупы из Seller API `/v3/finance/transaction/list` по операциям
  `OperationAgentDeliveredToCustomer` за дату отчета; не считать выкупы по
  статусу `delivered` из `/v2/posting/fbo/list`, потому что этот метод
  фильтрует отправления, а не факт выкупа за календарный день;
- брать Ozon расходы за вчера из всех операций Seller API
  `/v3/finance/transaction/list` за дату операции: комиссия
  `sale_commission`, логистика и эквайринг из `services`, реклама по операции
  `OperationMarketplaceCostPerClick`, хранение по
  `OperationMarketplaceServiceStorage`, возвраты/отмены/невыкупы по
  `OperationItemReturn`, кросс-докинг по `MarketplaceServiceItemCrossdocking`;
  итог расходов считать как `gross_amount - net_amount` и сверять с
  расшифровкой;
- после отключения Ozon `Звёздные товары` не удалять фактические
  `StarsMembership` из расходов за дату операции: они уже повлияли на выплату.
  Вместо общей строки активной программы разделять удержания по
  `posting.order_date` относительно `2026-07-29 13:56:22 UTC`:
  `заказы до отключения`, `первые 24 часа после отключения`,
  `после полных 24 часов`. Если последняя группа равна нулю, подписывать строку
  как поздние/переходные удержания и прямо указывать, что она не подтверждает
  повторное подключение программы;
- не использовать `/v1/finance/products/buyout` для обычных покупательских
  выкупов: по официальному обсуждению Ozon for dev этот метод относится к
  товарам, которые Ozon выкупил у продавца, а не к фактическим продажам
  покупателям;
- `/v1/finance/realization/by-day` является более прямым дневным источником
  реализации, но текущий ключ Vital Shevron вернул `HTTP 403: Data is
  available only with a Premium plus subscription`; поэтому в v3 используется
  `/v3/finance/transaction/list`;
- брать Ozon FBO отмены из Seller API `/v2/posting/fbo/list` за период
  `00:00-23:59` MSK только как операционный источник по отправлениям; для
  строгой отмены по дате отмены нужен отдельный проверенный источник;
- брать Ozon остатки из Seller API `/v4/product/info/stocks`;
- брать Ozon отзывы/вопросы сначала из официальных Seller API
  `/v1/review/count`, `/v1/review/list`, `/v1/question/count`,
  `/v1/question/list`; если официальный API недоступен для текущего ключа,
  использовать read-only LK/CDP fallback из `reviews_questions` и явно писать,
  что fallback видит только доступные в ЛК непросмотренные отзывы и новые
  вопросы;
- брать Ozon уведомления/сообщения из Messenger по
  `data/planning/ozon_messenger_runbook.md`: сначала официальный Seller API
  `/v3/chat/list` и `/v3/chat/history`, если их достаточно; если площадочные
  уведомления или UI-фильтры не видны через API, использовать LK/CDP websocket
  fallback; в отчете отделять вопросы покупателей, важные изменения площадки и
  шум;
- брать WB заказы/отмены из Statistics API `/api/v1/supplier/orders` с
  `flag=1`;
- для owner-facing метрик WB `Заказы, ₽` и `Выкупы, ₽` использовать
  `priceWithDisc`: это розничная сумма после скидки, соответствующая витрине
  аналитики WB. `finishedPrice` и `forPay` не подменяют эту сумму; `forPay`
  сохранять отдельно как финансовую диагностическую метрику;
- количество заказов WB считать по всем созданным строкам за дату, включая
  впоследствии отмененные; отмены показывать отдельной строкой. Дополнительно
  сохранять `active_orders`, `active_amount` и агрегаты доступных ценовых полей
  без raw-строк и данных покупателей для последующего разбора расхождений;
- брать WB выкупы/возвраты из Statistics API `/api/v1/supplier/sales` с
  `flag=1`; это оперативный предварительный источник, официально обновляется
  раз в 30 минут, `1 строка = 1 продажа/возврат = 1 товар`;
- в `--seller-v3` не запрашивать WB orders/sales за `today`, если отчет
  строится только за вчера: у Statistics API жесткий лимит, лишний второй
  запрос может привести к `HTTP 429` и потере нужного блока;
- брать WB финансовые расходы за вчера из Finance API
  `/api/finance/v1/sales-reports/list` с `period=daily`; использовать поля
  `retailAmountSum`, `forPaySum`, `deliveryServiceSum`, `paidStorageSum`,
  `paidAcceptanceSum`, `deductionSum`, `penaltySum`,
  `additionalPaymentSum`, `cashback*`, `paymentSchedule`, `bankPaymentSum`;
- издержки WB на приём платежей проверять отдельно через
  `/api/finance/v1/acquiring/list`, суммируя `acquiringFeeSum` и
  `acquiringFeeVatSum`;
- WB-расходы считать суммой положительных расходных статей, а не формулой
  `max(0, retailAmountSum - bankPaymentSum)`: `bankPaymentSum` может включать
  корректировки и зачисления за другие расчёты;
- отрицательные `deductionSum`, `cashback*`, `paymentSchedule` и другие
  отрицательные удержания не вычитать из расходов; показывать их отдельным
  блоком `корректировки/зачисления`;
- отдельно выводить две суммы: `после текущих расходов` =
  `retailAmountSum - положительные расходы - реклама`, и денежное начисление с
  корректировками = `bankPaymentSum - издержки приема платежей - реклама`;
- WB рекламу за вчера брать отдельно из Promotion API `/adv/v3/fullstats`,
  поле `sum`; добавлять как рекламный расход отдельно от отчета реализации и
  явно писать, что рекламный расход не сверен как часть `bankPaymentSum`;
- брать WB отзывы/вопросы за период и текущие требующие внимания из
  Customer Communication API;
- брать сводку по акциям из последних read-only/dry-run Ozon Elastic и WB
  actions reports;
- для причин по акциям читать детальные CSV последнего Ozon Elastic dry-run и
  WB actions dry-run:
  - Ozon `reason_code=no_stock` -> `нулевой остаток`;
  - Ozon `reason_code=below_min_price_threshold` -> `участие в акции выше
    порога/не проходит порог цены`;
  - WB `Акций=0` -> `нет акций для этого товара`;
  - WB `Причина` содержит `порога >` -> `участие в акции выше порога`;
- писать причины неучастия в акциях понятным русским текстом, без внутренних
  кодов вроде `skip_candidate` или `outside_promos`;
- писать `не подтверждено` для блоков без проверенного read-only источника.

Подтвержденное ограничение WB Statistics API: при `flag=1` метод возвращает
заказы или продажи за дату, равную `dateFrom`; время в `dateFrom` не важно.

## Regression hardening 2026-08-01

- точные отмены Ozon сначала брать из дневной аналитической метрики
  `cancellations`; FBO postings разрешены только как явно помеченный fallback,
  потому что не подтверждают точную дату отмены;
- отзывы и вопросы для дневного блока пагинировать и фильтровать по timestamp,
  преобразованному в `Europe/Moscow`; строки без подтвержденной даты не
  относить к отчетному дню;
- для Ozon и WB сохранять completeness/reconciliation расходов, включая
  нераспределенную сверочную разницу и warning;
- контрольная дата `2026-07-22` закреплена regression-тестами для отмен,
  границ суток и полноты расходов.
Источник: официальная документация WB API, раздел Main Reports.

Для исторической месячной юнит-экономики Statistics Sales API использовать
нельзя: это предварительный источник, который может не включать продажи с
неподтвержденной на момент обновления оплатой. Финансовый числитель и количество
изделий нужно брать из одного контура —
`/api/finance/v1/sales-reports/list` и
`/api/finance/v1/sales-reports/detailed`; `quantity` умножать на подтвержденный
`pack_qty`. Общие расходы без `nmId` не распределять по SKU без отдельного
согласованного правила аллокации.

Официальные источники WB:

- Main Reports: `https://dev.wildberries.ru/docs/openapi/reports`;
- Financial Reports: `https://dev.wildberries.ru/docs/openapi/financial-reports-and-accounting`;
- Promotion API: `https://dev.wildberries.ru/docs/openapi/promotion`.

## Recovery расходов WB 2026-07-23

В отчёте за `2026-07-22` расходы WB ошибочно показывались как `275,08 ₽`.
Причина: код применял `max(0, retailAmountSum - bankPaymentSum)`; при
`bankPaymentSum=23 782,98 ₽` и реализации `4 723,27 ₽` расходы площадки
обнулились из-за отрицательного `deductionSum=-21 640,68 ₽`.

Исправленная сверка по двум частям ежедневного отчёта:

- реализация: `4 723,27 ₽`;
- комиссия/удержание до логистики: `710,19 ₽`;
- логистика: `1 067,81 ₽`;
- хранение: `607,32 ₽`;
- удержание по графику платежей: `195,65 ₽`;
- реклама: `275,08 ₽`;
- издержки на приём платежей: `0 ₽` по
  `/api/finance/v1/acquiring/list`;
- текущие расходы: `2 856,05 ₽`;
- после текущих расходов: `1 867,22 ₽`;
- корректировка/зачисление по отрицательному удержанию: `21 640,68 ₽`;
- денежное начисление с корректировкой после рекламы: `23 507,90 ₽`.

Регрессионная проверка обязана подтверждать, что отрицательное удержание не
обнуляет комиссию, логистику, хранение и другие положительные расходы.

## Recovery неполного отчета 2026-07-18

Подтвержденные причины пропусков в отчете за `2026-07-17`:

- Ozon-заказы: V3 выполнял два последовательных запроса `/v1/analytics/data`;
  `HTTP 429` на лишнем запросе за текущий день затирал уже полученный результат
  за вчера;
- WB-остатки: использовался отключенный legacy endpoint
  `/api/v1/supplier/stocks`;
- Ozon/WB поставки: блок V3 был статической заглушкой, хотя read-only адаптеры
  уже существовали;
- перечень проблем источников не включал заказы и остатки, поэтому отчет не
  перечислял все отсутствующие блоки.

Исправленный обязательный маршрут:

- для V3 запрашивать Ozon `/v1/analytics/data` только за завершенный вчерашний
  день; ошибка отдельного периода не должна стирать успешно полученный период;
- WB-остатки читать через
  `POST /api/analytics/v1/stocks-report/wb-warehouses`, агрегировать по `nmId`
  по всем складам и сопоставлять с unified catalog по `wb_nm_id`;
- отсутствие `nmId` в stock response показывать отдельно как
  `нет строки в источнике`, а не как подтвержденный нулевой остаток;
- Ozon-поставки читать через `/v3/supply-order/list` и
  `/v3/supply-order/get`; WB-поставки — через
  `supplies-api.wildberries.ru/api/v1/supplies`;
- для WB не выдумывать отдельные стадии `в пути` и `на складе приемки`, которых
  нет в подтвержденном наборе статусов API; выводить `0` и пояснять ограничение;
- сбой любого источника поставок переводит V3 в `warning`;
- в owner-facing таблице каталога не использовать число строк со статусом
  `confirmed` как показатель полноты mapping: после карточных apply часть
  связанных товаров имеет статус `ozon_wb`, а marketplace-only товары уже
  могут иметь полностью согласованный `internal_sku`;
- отдельно показывать: завершенность идентификации целевого ассортимента
  `chev/nash/loop`, число связанных пар Ozon+WB и фактическое активное
  присутствие только на одной площадке;
- статусы `ozon_only`/`wb_only` после завершенного owner-review не называть
  непросмотренным или несопоставленным хвостом: это подтвержденное состояние
  присутствия товара на одной площадке.

Live verify `2026-07-18`:

- run: `daily_morning_report_v3_repair_20260718T081625`;
- Ozon-заказы: `status=ok`, `100` шт., `59 003 ₽`;
- WB-остатки: `status=ok`, `771` складская строка, `370` nmID, `4 805` шт.;
- Ozon-поставки: `6` текущих, из них `1` в пути и `5` на подтверждении;
- WB-поставки: `4` текущих, из них `3` готовы к отгрузке и `1` на приемке;
- оставшийся общий `warning` относится к известным ограничениям Ozon FBO
  отмен и Ozon отзывов/вопросов, а не к заказам, остаткам или поставкам.

Нормализация owner-facing mapping-сводки `2026-07-18`:

- run: `daily_morning_report_v3_mapping_normalized_20260718T083151`;
- целевой ассортимент идентифицирован: `да`, `645/645` товарных записей имеют
  внутренний артикул;
- связанные пары Ozon+WB: `397`;
- активные только Ozon: `201`, активные только WB: `111`;
- технический счетчик `confirmed=179` сохранен только в JSON для обратной
  совместимости и больше не выводится владельцу как показатель полноты.

## Recovery противоречивой сводки 2026-07-29

Проверка отчета за `2026-07-28` выявила не ошибки первичных финансовых данных,
а три ошибки owner-facing трактовки:

- WB `in_promos=361` ошибочно назывался числом участвующих товаров. Это число
  товаров, присутствующих в файлах доступных активных акций. Фактическое
  участие требует одновременно положительного статуса в файле акции и текущей
  скидки не ниже `Скидка до порога`;
- количество пакетов на согласование считалось по всем каталогам
  `data/pending`, включая закрытые. Источником должен быть
  `run_approvals_status(..., include_closed=False)`;
- `non_target_or_deferred_products=61` показывался как отдельная добавка к
  `706` товарам каталога, хотя это подмножество общего числа.

Обязательная логика после исправления:

- расчет участия WB выполняется общей функцией
  `wb_actions_report_stats()` из CSV свежего WB actions dry-run; бот ручной
  акции и ежедневный отчет используют одну реализацию;
- отдельно выводятся: всего товаров, доступна активная акция, фактически
  участвует, доступна акция, но скидка недостаточна, активная акция
  недоступна, результат после применения текущего dry-run;
- для Ozon отдельно выводятся текущие участники, кандидаты на добавление,
  кандидаты на удаление и расчетный результат после dry-run;
- закрытые approval/pending-пакеты не должны попадать в операционный риск;
- `Из них вне целевого ассортимента` всегда обозначается как подмножество
  общего каталога;
- для WB `priceWithDisc` в оперативных выкупах и
  `retailAmountSum`/`finishedPrice` в финансовой реализации имеют разные
  расчетные базы. Их расхождение поясняется в отчете, а не маркируется как
  ошибка;
- при LK/CDP fallback для Ozon отзывы за период называть только видимыми
  непросмотренными и прямо предупреждать о возможном недосчете.

Live verify `2026-07-29`:

- исправленный run:
  `daily_morning_report_v3_20260729T072628`;
- первичные цифры заказов, выкупов и расходов отдельно повторно подтверждены
  Seller API / Statistics API / Finance API;
- WB: `539` товаров в расчете, `361` доступны активные акции, `133`
  фактически участвуют, `228` имеют доступную акцию, но недостаточную скидку,
  `178` не имеют доступной активной акции;
- открытые пакеты согласования: `0`;
- точная расшифровка расходов выводится с копейками, чтобы округление каждой
  статьи отдельно не создавало визуальное расхождение с итогом;
- тесты отчетного и Telegram-контура: `97 passed`.
