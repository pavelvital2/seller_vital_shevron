[project]
name = "seller-agent"
version = "0.1.0"
description = "Generic marketplace seller agent and task runner"
requires-python = ">=3.11"
dependencies = ["openpyxl>=3.1"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
